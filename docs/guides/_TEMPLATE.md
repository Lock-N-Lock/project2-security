<!--
이 파일을 복사해서 docs/guides/<트랙소문자>-<영역>.md 로 작성하세요.
예: docs/guides/a-infra-terraform.md
모든 섹션을 채우되, 특히 "다른 트랙과의 인터페이스"는 통합의 핵심이니 반드시 작성합니다.
-->

# [트랙X] 영역명 가이드

| 항목 | 내용 |
|------|------|
| 트랙 | (예: A — 인프라·IaC) |
| 담당 | (이름) |
| 디렉토리 | (예: `infra/terraform/`) |
| 최종 수정 | (YYYY-MM-DD) |

## 1. 개요

이 영역이 프로젝트에서 무엇을 책임지는지 2~3문장으로 설명합니다.

## 2. 디렉토리·파일 구조

```
(이 영역의 파일 트리와 각 파일이 무엇을 하는지 한 줄 주석)
```

## 3. 핵심 동작 흐름

코드가 어떤 순서로 무엇을 하는지 설명합니다. 필요 시 단계별 목록이나 간단한 흐름도를 사용합니다.

1. (단계 1)
2. (단계 2)
3. (단계 3)

## 4. 다른 트랙과의 인터페이스 (필수)

내가 **받는 입력**과 **내보내는 출력**을 명시합니다. 통합 충돌의 90%가 여기서 갈립니다.

**내가 받는 것 (입력)**

| 어디서 | 무엇을 | 형식·경로 |
|--------|--------|-----------|
| (예: A 트랙) | (예: EC2 private IP) | (예: terraform output) |

**내가 내보내는 것 (출력)**

| 어디로 | 무엇을 | 형식·경로 |
|--------|--------|-----------|
| (예: C·D 트랙) | (예: Bastion IP, SG ID) | (예: `outputs.tf`) |

## 5. 실행·테스트 방법

```bash
(이 영역을 돌려보는 최소 명령어)
```

## 6. 트러블슈팅 / 알려진 이슈

- (겪은 문제와 해결 방법 — 발표의 "어떤 부분이 어려웠고 어떻게 해결했는가"에 그대로 활용)

## 7. 변경 이력

| 날짜 | 변경 내용 | 작성자 |
|------|-----------|--------|
| | | |